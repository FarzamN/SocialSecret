export const EmailRegix = /^\w+([.-]?\w+)*@\w+([.-]?\w+)*(\.\w{2,3})+$/;

export const baseUrl = 'https://adv-express.vercel.app';

export const apiUrl = `${baseUrl}/api/`;
export const imageUrl = `${baseUrl}/`;
